%{
function [suma,iloczyn] = suma_iloczyn(x) 
    suma = sum(x);
    iloczyn=1;
    for i=0:length(x)
        iloczyn = iloczyn * x(i);
    end
return
%}
a = [1.,2.,3.,4.,5.,6.]
suma_iloczyn(a)

function [suma,iloczyn] = suma_iloczyn(x) 
    suma = sum(x);
    iloczyn=1.;
    for i=1:length(x)
        iloczyn = iloczyn * x(i);
    end
end





